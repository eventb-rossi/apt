#!/usr/bin/make -f
export PYBUILD_NAME=eventb-to-txt

%:
	dh $@ --with python3 --buildsystem=pybuild

# Upstream ships a pytest suite; the installed console script is exercised as a
# smoke test after installation instead, so the build stays free of test deps.
override_dh_auto_test:
